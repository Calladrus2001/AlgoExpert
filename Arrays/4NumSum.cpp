#include <iostream>
#include <vector>
#include <map>
using namespace std;

vector<vector<int>> fourNumberSum(vector<int> array, int targetSum)
{

  // store the sum of all pairs in a map
  for (int i = 0; i < array.size(); i++)
  {
    for (int j = i + 1; j < array.size(); i++)
    {
    }
  }
  return {};
}
